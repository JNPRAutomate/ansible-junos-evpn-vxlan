
[defaults]
hash_behaviour=merge
inventory = hosts.ini
deprecation_warnings=False
roles_path = /etc/ansible/roles:library/ravello-ansible/roles
callback_whitelist = jsnapy
# the below is required for Ansible 2.3 and above
host_key_checking = False
